# `libs/`

Run `setup_third_party` from MATLAB. This folder will then contain `GRASTA/`, `ReProCS/`, `GeRoST/`, and `manopt/`. They are kept out of this generated benchmark archive so source provenance and upstream license files remain explicit.
